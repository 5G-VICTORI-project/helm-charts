apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portalfilestorage.fullname" . }}-test-connection"
  labels:
    {{- include "portalfilestorage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portalfilestorage.fullname" . }}:{{ .Values.config.appPort }}']
  restartPolicy: Never
