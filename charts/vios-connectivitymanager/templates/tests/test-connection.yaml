apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "connectivitymanager.fullname" . }}-test-connection"
  labels:
    {{- include "connectivitymanager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "connectivitymanager.fullname" . }}:{{ .Values.config.appPort }}']
  restartPolicy: Never
