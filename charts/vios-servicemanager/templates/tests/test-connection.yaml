apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "servicemanager.fullname" . }}-test-connection"
  labels:
    {{- include "servicemanager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "servicemanager.fullname" . }}:{{ .Values.config.appPort }}']
  restartPolicy: Never
